<div class="panel panel-warning">
  <div class="panel-heading">
    <h2 class="panel-title"><span class='glyphicon glyphicon-list'></span> {{ 'sylius.newest'|trans }}</h2>
  </div>
  <div class="panel-body">
    <ul>
	    {% for product in products %}
	        <li>
	            <a href="{{ path(product) }}" class="btn btn-link">
	                <img class="thumbnail" src="{{ product.image ? product.image.path|imagine_filter('sylius_small') : 'http://placehold.it/50x40' }}" alt="{{ product.name }}" />
	                <h4>{{ product.name }}</h4>
	            </a>
	        </li>
	    {% endfor %}
	</ul>
  </div>
</div>